// Stage 7: match arms with block bodies (RFC-0018).
// Arms can use either `=> expr` or `=> { stmts* expr? }`.

enum Shape {
    Circle { radius: f64 },
    Rectangle { width: f64, height: f64 },
}

let s: Shape = Shape::Circle { radius: 3.0 };

// Block arm with a local binding and computation.
let area: f64 = match s {
    Shape::Circle { radius } => {
        let r = radius;
        r * r
    },
    Shape::Rectangle { width, height } => width * height,
};

// Block arm producing unit (no tail expression).
let msg: Perhaps<i64> = Perhaps::Some { value: 1 };
match msg {
    Perhaps::Some { value } => {
        let v = value;
    },
    None => {},
};

// Mixed: one arm is a bare expr, one is a block.
let ok: Result<i64, String> = Result::Ok { value: 5 };
let n: i64 = match ok {
    Result::Ok { value } => value,
    Result::Err { error } => {
        let fallback = 0;
        fallback
    },
};
