// Generic utilities imported by main — tests that polymorphic calls survive elaboration.

pub fun identity<T>(x: T) -> T { x }

pub fun first<T>(a: T, b: T) -> T { a }

pub fun apply<T>(f: (T) -> T, x: T) -> T { f(x) }
