struct Counter {
    value: i64,
}

impl Counter {
    fun increment(&mut self) {
        self.value += 1;
    }

    fun current(&self) -> i64 {
        self.value
    }
}

fun main() {
    let mut counter = Counter { value: 0 };
    counter.increment();
    counter.increment();
    assert(counter.current() == 2);
}
