// Positive: local struct is visible anywhere within its declaring block,
// including after its definition site and in nested expressions.

fun use_local_struct() -> i64 {
    struct Pair { a: i64, b: i64 }
    let p = Pair { a: 10, b: 32 };
    p.a + p.b
}
