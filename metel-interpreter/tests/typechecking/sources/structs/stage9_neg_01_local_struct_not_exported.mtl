// Negative: a struct declared inside a function body must not be visible
// in a different function's body.

fun declare_it() {
    struct Local { x: i64 }
}

fun use_it() -> i64 {
    let v = Local { x: 1 };  // ERROR[T0003]
    v.x
}
