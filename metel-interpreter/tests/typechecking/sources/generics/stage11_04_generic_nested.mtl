// Positive: nested generic types — Box<Pair<i64, Bool>>.

struct Box<T> {
    value: T,
}

struct Pair<A, B> {
    first: A,
    second: B,
}

fun make_boxed_pair(a: i64, b: Bool) -> Box<Pair<i64, Bool>> {
    Box { value: Pair { first: a, second: b } }
}

fun get_pair(b: Box<Pair<i64, Bool>>) -> Pair<i64, Bool> {
    b.value
}
