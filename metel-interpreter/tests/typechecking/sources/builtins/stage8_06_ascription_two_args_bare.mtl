// Param types from the callee signature flow into argument construction,
// so bare empty arrays resolve without requiring ascription.

fun zip_lengths(a: i64[], b: String[]) -> i64 {
    a.len() + b.len()
}

let _: i64 = zip_lengths([], []);
