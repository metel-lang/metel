// Negative test: casting to a type that has no From impl must be rejected.
// TYPECHECK_ERROR[cannot cast `Foo` to `Bar`]

struct Foo { x: i64 }
struct Bar { y: i64 }

fun main() {
    let f = Foo { x: 1 };
    let b = f as Bar; // no From<Foo> for Bar — type error
}
