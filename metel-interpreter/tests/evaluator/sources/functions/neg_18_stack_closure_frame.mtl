// An anonymous closure passed as an argument and then called must appear in
// the stack trace as "<closure>".

fun apply(f: () -> Int) -> Int {
    f()
}

fun main() {
    apply(() -> Int { 1 / 0 });
}

// RUNTIME_ERROR[in <closure>]
