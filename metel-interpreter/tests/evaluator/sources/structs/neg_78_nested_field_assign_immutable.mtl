// Field assignment to a nested field chain rooted at a plain let binding is a type error.
// TYPECHECK_ERROR[immutable binding]
struct Inner { x: Int }
struct Outer { inner: Inner }

fun main() {
    let s = Outer { inner: Inner { x: 1 } };
    s.inner.x = 42;
}
